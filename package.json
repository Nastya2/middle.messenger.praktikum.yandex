{
  "name": "chat",
  "source": "src/index.html",
  "scripts": {
    "start": "node src/server.js ",
    "build": "parcel build src/**/*.pug --dist-dir public --no-cache",
    "server": "parcel src/pages/home/modules/errors/error404.pug"
  },
  "devDependencies": {
    "@parcel/transformer-pug": "^2.3.2",
    "@parcel/transformer-sass": "^2.3.2",
    "express": "^4.17.3",
    "parcel": "^2.3.2"
  },
  "dependencies": {
    "normalize.css": "^8.0.1"
  },
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "engines": {
    "node": ">= 12.0.0"
  }
}
